## What

<!-- What does this PR do? One sentence. -->

## Why

<!-- Why is this change needed? Link to issue if applicable. Closes #N -->

## Test plan

- [ ] Loaded unpacked in Chrome (`dist/chrome/`)
- [ ] Loaded in Firefox (`about:debugging`)
- [ ] Omnibox `hh` trigger works
- [ ] Options page saves/loads correctly
- [ ] `npm run lint` passes

## Screenshots (if UI change)
